// union types
let age: number | string | boolean = 20;

age = "20";
let myvar = 10;
// union types of Array
// we can store both string & both number
let mixedArr: (string | number)[] = [1, 'Two'];

// common types of Array

let commonArray: string[] | number[] = [1, 2]
commonArray = ['two']

let day: "Saturday" | "Sunday" | "Monday" = "Saturday";


