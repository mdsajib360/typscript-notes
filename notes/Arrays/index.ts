let arr: number[] = []

let nameOfStudents: string[] = ["Rafi", "Rakif"]

let mixedArray: (number | string)[] = [1, 'Two', 3, 'Four']
console.log(mixedArray);

let threeDArray: number[][][] = [
    [[1, 2, 3,]],
    [[1, 2, 3,]],
    [[122,124 ]]
]

threeDArray.map(twoD => console.log(twoD))
